# greet if name start with s

list = ["chirag", "suman", "suresh", "mahesh"]

for name in list:  # we can directly put "name" and "startswith" is function
    if name.startswith("s"):
        print(name)
